[package]
name = "utils"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
logging = {path = '../logging'}

atomic-traits = "0.3"
num-traits = "0.2"
slave-pool = "0.2"

[dev-dependencies]
static_assertions = "1.1"

[target.'cfg(loom)'.dependencies]
loom = "0.5"
