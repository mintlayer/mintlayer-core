# Environment file for mintlayer-api-web-server@<chain>.service
# The web server requires Postgres connection settings and node RPC access in ARGS.
# See `mintlayer-api-web-server --help` for all options.

# ARGS=
# RUST_LOG=info
