# Environment file for mintlayer-api-blockchain-scanner@<chain>.service
# The scanner requires Postgres connection settings and node RPC access in ARGS.
# See `mintlayer-api-blockchain-scanner-daemon --help` for all options.

# ARGS=
# RUST_LOG=info
