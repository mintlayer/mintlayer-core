# Environment file for mintlayer-node@<chain>.service
# Copy or edit as needed; the unit loads it if present (optional).
# See `mintlayer-node-daemon --help` for the full set of options; every option
# can also be set via the MAINNET_NODE / TESTNET_NODE environment variables.

RUST_LOG=info
# RUST_BACKTRACE=full

# Example: custom p2p port
# ARGS=--p2p-port 30313
