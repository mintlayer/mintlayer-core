# Environment file for mintlayer-wallet-rpc@<chain>.service
# The daemon requires at least the chain and the wallet file path in ARGS.
# Example:
#   ARGS=--wallet-file /var/lib/mintlayer/my-wallet.mlt
# See `mintlayer-wallet-rpc-daemon --help` for all options.

# ARGS=
# RUST_LOG=info
