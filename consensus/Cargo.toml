[package]
edition = "2021"
license = "MIT"
name = "consensus"
version = "0.1.0"

[dependencies]
common = {path = '../common'}
chainstate-types = {path = '../chainstate-types'}
chainstate-storage = {path = '../chainstate-storage'}

thiserror = "1.0"
num = "0.4.0"

[dev-dependencies]
rstest = "0.15"

