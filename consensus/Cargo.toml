[package]
edition = "2021"
license = "MIT"
name = "consensus"
version = "0.1.0"

[dependencies]
common = {path = '../common'}
utils = {path = "../utils"}
chainstate-types = {path = '../chainstate/types'}
pos_accounting = {path = '../pos_accounting'}
utxo = {path = '../utxo'}

thiserror.workspace = true
num = "0.4.0"

[dev-dependencies]
rstest = "0.16"
